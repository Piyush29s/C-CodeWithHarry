#include<stdio.h>

int main()
{
    // int a=4, b, c; //Type declaration instruction
    // b=c=a;
    // printf("The value of a is %d", a);
    // printf("\nThe value of b is %d", b);
    // printf("\nThe value of c is %d", c);

    // Instructions are executed one by one i.e line by line

    float a = 1.1;
    float b = a+8.9;
    printf("The valur of b is %.1f", b);
    return 0;
}